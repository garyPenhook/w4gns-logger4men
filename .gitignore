# Local build artifacts
/bin/
/w4gns-logger

# Local log databases and SQLite sidecar files
*.db
*.db-shm
*.db-wal

# Editor and operating-system files
.DS_Store

# QRZ Logbook API key and XML lookup credentials (secret, never commit)
qrz.comAPIkey
qrz.comXMLlogin

# World Radio League API key (secret, never commit)
worldradioleague.comAPIkey

# LoTW/TQSL station location and signing passphrase (secret, never commit)
lotw.station
lotw.pass

# LoTW website login (confirmation sync; secret, never commit)
lotw.login
lotw.webpass
